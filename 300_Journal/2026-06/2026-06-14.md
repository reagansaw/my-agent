# 學習日記 2026-06-14

> 寫於：2026-06-14

---

## 今天做了什麼

一整天泡在 aigoal-ops 的 landing builder，把它從「會生漂亮的靜態頁」一路打磨成「會生功能性 funnel」。修了一輪真問題：手機實測（自己用 375px 渲染走一遍，nav/篩選/表單全綠）、治 AI 亂編（房產頁誠實分從 2 拉到 5）、補了一個儲存型 XSS 安全洞、加了品質考卷、prompt caching、版面安全鐵律、互動組件庫。一件一件上線。

## 有趣的時刻

最大的「哦原來這樣」：**出品不夠好看，不是模型不夠強，是 context（prompt + 素材）的問題。** 同一個 Sonnet，光改 prompt、給對素材，出品就跳一級——換模型反而是最後才動的旋鈕。而且做了考卷後，一跑就抓到「AI 亂編」，補完又抓到「頁太豐富撐爆 token 截斷崩版」，一個改動牽出兩個洞。

## 反思

**做得不錯的**：有了考卷之後不再蒙眼改——「改 → 測 → 抓到副作用 → 補 → 再測 → 才上線」這條紀律跑起來了。

**可以更好的**：舊頁要重新生成才吃到新規則、PEXELS key 還沒加、track_secret 安全洞還沒補——尾巴沒收乾淨。

## 明天的一個改進

做完馬上實測，不要只看截圖就當好了。

## 明天要做的事

- [ ] 補 track_secret 安全洞（安全債先清，別一直堆新功能）
- [ ] 加 PEXELS key + 重生一張舊頁，實測真照片有沒有進來
- [ ] 跑完整 eval 基線，存一個基準分數

---
*由 /journal skill 生成*
